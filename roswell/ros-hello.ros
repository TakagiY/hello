#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  (unless (find-package :hello)
    (ql:quickload :hello :silent t)))

(defpackage :ros.script.ros-hello.3730571621
  (:use :cl))
(in-package :ros.script.ros-hello.3730571621)

(defun main (&rest argv)
  (declare (ignorable argv))
  (hello:hello argv))

;;; vim: set ft=lisp lisp:
