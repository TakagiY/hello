#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  ;;#+quicklisp (ql:quickload '() :silent t)
  )

(defpackage :ros.script.ros-hello.3730566510
  (:use :cl))
(in-package :ros.script.ros-hello.3730566510)

(defun main (&rest argv)
  (declare (ignorable argv))
  (hello argv))
;;; vim: set ft=lisp lisp:
